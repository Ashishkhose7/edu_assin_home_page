function rating(){
    var elems = document.querySelectorAll(".star-icon");
    var index = 0,
      length = elems.length;
      elems[0].style.color = "red";
    // for (; index < length; index++) {
    //   elems[index].style.color = "red";
    // }
    console.log(elems)
}
